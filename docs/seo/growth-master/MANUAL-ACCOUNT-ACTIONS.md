# Manual account actions

## Bing Webmaster Tools

Founder must confirm whether `nutrithrive.com.au` is already verified and record the active method before any verification file is added. Check sitemap submission/crawl/errors, crawl errors, URL Inspection for priority PDPs, AI Performance access and IndexNow reporting. Export AI Performance for 30 days and 3 months if available. Do not create duplicate ownership verification.

## IndexNow gate

After Bing ownership is confirmed, choose a key-storage and deployment path that exposes only the public key file. Submit canonical public URLs only when added, meaningfully updated or deleted. Exclude parameters, internal search, cart, checkout, staging and redirects. Log URL, action, timestamp and response only—never customer information. No key or submission code was created in this run.

## Google Search Console

Export query × page data for Curry, Darjeeling and Soap with clicks, impressions, CTR, position, date range, country and device. Inspect the three PDPs and the priority articles. Do not infer cannibalisation from separate query and page exports.

## Google Merchant Center

Confirm property ownership, free-listing status, existing product sources/feeds, approvals and disapprovals, Australian shipping settings, return policy, AUD, target country, product images, identifiers, price/availability mismatches and landing-page errors. Do not create a duplicate feed first.

## Bing Merchant / Places

Confirm whether feeds or listings already exist, their ownership, freshness and errors. Verify business name, ABN, address, phone, email and opening hours against the founder's records.

## CDN / WAF

From hosting dashboards and logs, verify HTTP 200 access to public HTML, structured data and important images for Googlebot, Bingbot, DuckDuckBot, OAI-SearchBot, Claude-SearchBot, Claude-User and PerplexityBot. Match vendor user agents with current published IP ranges before creating allow rules. Do not weaken general security controls.

## GA4 controlled test

Use DebugView or equivalent for one non-charged journey: landing page → article → product → add to cart → cart → begin checkout. Check `view_item`, `blog_product_click`, `add_to_cart`, `view_cart`, `begin_checkout`, duplicate events, consent behaviour, self/payment referrals and source/medium preservation. Do not complete a charge or change payment code without explicit approval.

## Business facts needing founder confirmation

Confirm 09:00–23:00 seven-day opening hours currently present in LocalBusiness schema; confirm the current Google review count/rating; supply product-specific review evidence if Product ratings are desired; confirm paid shipping below $79, returns, GTIN/MPN and product availability before Merchant/schema additions.

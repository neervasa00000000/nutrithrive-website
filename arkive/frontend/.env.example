# Copy to .env and fill in (optional for local dev — MetaMask/injected wallets work without this)
# Required for WalletConnect mobile wallet QR scanning
VITE_WALLETCONNECT_PROJECT_ID=
VITE_ARWEAVE_KEY=

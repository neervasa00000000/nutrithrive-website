/**
 * Single source for the public PayPal JS SDK client id (storefront).
 * Keep in sync with Netlify env PAYPAL_CLIENT_ID for the same PayPal app.
 * Change only here when rotating the client id sitewide.
 */
(function (w) {
    if (!w) return;
    w.NUTRITHRIVE_PAYPAL_CLIENT_ID = "AWtclBnz1gQWQne-vS-OnExG1-Rl7Tj01nE8J1j7aZsLItOJvecwRVCpG757OrJ3QCf65w7q9i2bSgVi";
})(typeof window !== "undefined" ? window : undefined);
